# Сглаживатель поверхности и логика оптимизации формы

Этот документ фиксирует математическую постановку и алгоритмическую логику, реализованные в текущем коде проекта для:

1. дешёвого сглаживания поверхности по нормалям;
2. оптимизации формы на основе CMA-ES и внешнего МКЭ-решателя.

Документ написан в формате, близком к тексту статьи: с обозначениями, формулами и описанием вычислительного контура.

## 1. Обозначения

Рассмотрим сетку с узлами

$$
\mathbf{r}_i \in \mathbb{R}^3,
$$

где $i$ — номер узла.

Для подмножества подвижных узлов заданы нормали

$$
\mathbf{n}_i \in \mathbb{R}^3,
\qquad
\hat{\mathbf{n}}_i = \frac{\mathbf{n}_i}{\|\mathbf{n}_i\|}.
$$

Для каждого подвижного узла $i$ строится множество соседей $\mathcal{N}(i)$ по элементной связности: если два подвижных узла входят в один конечный элемент, то они считаются соседями.

Вектор смещения узла всегда представляется в виде сдвига вдоль нормали:

$$
\Delta \mathbf{r}_i = s_i \hat{\mathbf{n}}_i,
$$

где $s_i \in \mathbb{R}$ — скалярный коэффициент смещения.

---

## 2. Сглаживание поверхности вдоль нормалей

### 2.1. Основная идея

Сглаживатель не двигает узлы произвольно в пространстве, а разрешает смещение только вдоль локальной нормали. Это резко удешевляет шаг сглаживания и делает его совместимым с параметризацией формы через нормальные сдвиги.

Алгоритм основан на projected Laplacian smoothing:

1. для узла $i$ вычисляется локальный центроид соседей;
2. строится лапласианоподобный вектор к этому центроиду;
3. из этого вектора берётся только проекция на нормаль узла.

### 2.2. Локальный центроид

Для каждого подвижного узла $i$ вычисляется средняя позиция соседей:

$$
\mathbf{c}_i = \frac{1}{|\mathcal{N}(i)|} \sum_{j \in \mathcal{N}(i)} \mathbf{r}_j.
$$

Тогда локальный "лапласиан" задаётся как

$$
\mathbf{l}_i = \mathbf{c}_i - \mathbf{r}_i.
$$

### 2.3. Проекция на нормаль

Поскольку разрешено смещение только вдоль нормали, скалярный коэффициент вычисляется как проекция лапласиана на единичную нормаль:

$$
s_i = \hat{\mathbf{n}}_i^{\mathsf T} \mathbf{l}_i.
$$

После введения весового коэффициента $\lambda$ получаем рабочую формулу шага:

$$
s_i^{(\lambda)} = \lambda \, \hat{\mathbf{n}}_i^{\mathsf T}(\mathbf{c}_i - \mathbf{r}_i).
$$

И, соответственно,

$$
\Delta \mathbf{r}_i^{(\lambda)} = s_i^{(\lambda)} \hat{\mathbf{n}}_i.
$$

### 2.4. Ограничение максимального шага

Чтобы исключить неустойчивые скачки поверхности, скаляр ограничивается по модулю:

$$
s_i \leftarrow \operatorname{clip}(s_i, -s_{\max}, s_{\max}).
$$

В коде используется либо явное значение `max_step`, либо автоматически предложенное ограничение:

$$
s_{\max} = \max(5 \cdot 10^{-4},\; 0.015\, \ell_{\mathrm{char}}),
$$

где $\ell_{\mathrm{char}}$ — характерная длина поверхностного ребра, оценённая как медиана длин поверхностных рёбер.

### 2.5. Двухпроходное сглаживание типа Taubin

Один цикл сглаживания состоит из двух возможных проходов:

1. прямой сглаживающий проход с коэффициентом $\lambda > 0$;
2. обратный компенсирующий проход с коэффициентом $\mu < 0$.

Тем самым одна итерация может быть записана как:

$$
\mathbf{r}_i^{\,*} = \mathbf{r}_i + \lambda \, \hat{\mathbf{n}}_i \hat{\mathbf{n}}_i^{\mathsf T}(\mathbf{c}_i - \mathbf{r}_i),
$$

$$
\mathbf{r}_i^{\,\text{new}} = \mathbf{r}_i^{\,*} + \mu \, \hat{\mathbf{n}}_i \hat{\mathbf{n}}_i^{\mathsf T}(\mathbf{c}_i^{\,*} - \mathbf{r}_i^{\,*}).
$$

Здесь $\mu$-проход играет роль частичной компенсации усадки поверхности, характерной для простого лапласиан-сглаживания.

### 2.6. Параметры, используемые в реализации

Для текущей "классической" версии smoother’а в коде зафиксированы следующие значения по умолчанию:

- $\lambda = 0.35$,
- $\mu = -0.37$,
- относительный предел шага: $0.015\,\ell_{\mathrm{char}}$.

В UI в качестве рабочих стартовых значений были выбраны:

- $\lambda = 0.25$,
- $\mu = -0.26$.

### 2.7. Что именно сохраняется после сглаживания

Для визуализации и дальнейшего анализа результат сохраняется как набор скалярных сдвигов и векторов смещения:

$$
\Delta \mathbf{r}_i = s_i \hat{\mathbf{n}}_i.
$$

В JSON также записываются:

- `original_coords` — исходные координаты;
- `smoothed_coords` — координаты после смещения;
- `scalar` — скалярный коэффициент вдоль нормали;
- `delta` — полный вектор смещения в $\mathbb{R}^3$.

---

## 3. Параметризация задачи оптимизации формы

### 3.1. Границы допустимых смещений

Перед запуском оптимизации для каждой степени свободы формы извлекаются допустимые интервалы смещения:

$$
a_i \in [a_i^{\min}, a_i^{\max}], \qquad i = 1,\dots,n.
$$

Эти интервалы формируются внешним инструментом `acli bs-shift-boundary`, который по сетке строит:

- `normals.txt` — нормали в подвижных узлах;
- `shifts.txt` — нижние и верхние границы смещений.

Таким образом, размерность задачи оптимизации равна числу доступных коэффициентов:

$$
n = \text{dimension} = |\{a_i\}|.
$$

### 3.2. Нормализованное пространство поиска

Сам CMA-ES работает в нормализованном пространстве:

$$
\mathbf{x} \in [0,1]^n.
$$

Переход к физическим коэффициентам смещения выполняется по формуле

$$
a_i = a_i^{\min} + x_i\,(a_i^{\max} - a_i^{\min}).
$$

Именно вектор

$$
\mathbf{a} = (a_1,\dots,a_n)
$$

записывается в файл `shift_coefficients_*.txt` и затем передаётся внешнему решателю.

---

## 4. Чёрный ящик fitness-функции

### 4.1. Общая постановка

Функция качества формы в проекте вычисляется не аналитически, а через внешний МКЭ-конвейер `acli bs-form-opt`. Поэтому оптимизация решает задачу чёрного ящика:

$$
\min_{\mathbf{x} \in [0,1]^n} f(\mathbf{x}),
$$

где

$$
f(\mathbf{x}) = \operatorname{Goal}\bigl(\mathrm{bs\text{-}form\text{-}opt}(\mathbf{a}(\mathbf{x}))\bigr).
$$

### 4.2. Последовательность одного вычисления fitness

Для одного кандидата $\mathbf{x}$ выполняются следующие шаги:

1. денормализация в физические коэффициенты $\mathbf{a}$;
2. сохранение коэффициентов в текстовый файл;
3. создание конфигурационного JSON для `bs-form-opt`;
4. запуск внешнего решателя;
5. чтение значения целевой функции из `goals.txt`.

Схематически:

$$
\mathbf{x}
\;\longrightarrow\;
\mathbf{a}
\;\longrightarrow\;
\texttt{shift\_coefficients.txt}
\;\longrightarrow\;
\texttt{bs-form-opt}
\;\longrightarrow\;
\texttt{goals.txt}
\;\longrightarrow\;
 f(\mathbf{x}).
$$

Если внешний вызов завершается с ошибкой, fitness принудительно полагается равным

$$
 f(\mathbf{x}) = +\infty,
$$

что автоматически делает решение нежизнеспособным для эволюционного отбора.

### 4.3. Параллельная оценка

Для ускорения CMA-ES поддерживается пакетная оценка популяции. В этом режиме несколько вызовов `bs-form-opt` запускаются параллельно в независимых рабочих каталогах. Математически это не меняет постановку задачи, но сокращает wall-clock time поколения:

$$
\{\mathbf{x}_1,\dots,\mathbf{x}_\lambda\}
\;\mapsto\;
\{f(\mathbf{x}_1),\dots,f(\mathbf{x}_\lambda)\}
$$

с частичной параллельной реализацией вычислений.

---

## 5. CMA-ES в данной реализации

### 5.1. Общая идея

Используется стратегия Covariance Matrix Adaptation Evolution Strategy (CMA-ES), предназначенная для непрерывной оптимизации в задачах со сложной, неаналитической и дорогостоящей целевой функцией.

Алгоритм поддерживает многомерное нормальное распределение поиска:

$$
\mathbf{x} \sim \mathcal{N}(\mathbf{m}, \sigma^2 \mathbf{C}),
$$

где

- $\mathbf{m}$ — текущее среднее распределения,
- $\sigma$ — глобальный размер шага,
- $\mathbf{C}$ — ковариационная матрица.

### 5.2. Инициализация

В коде среднее стартует из центра нормализованного диапазона:

$$
\mathbf{m}^{(0)} = (0.5, 0.5, \dots, 0.5)^{\mathsf T}.
$$

Начальная ковариационная матрица — единичная:

$$
\mathbf{C}^{(0)} = \mathbf{I},
$$

а начальный шаг задаётся параметром `sigma`.

Размер популяции выбирается по эвристике Hansen:

$$
\lambda = 4 + \lfloor 3 \ln n \rfloor,
$$

с дополнительным ограничением в коде:

$$
8 \le \lambda \le 256.
$$

Число родителей берётся как

$$
\mu = \lfloor \lambda / 2 \rfloor.
$$

### 5.3. Генерация потомков

На каждой итерации генерируются векторы

$$
\mathbf{z}_k \sim \mathcal{N}(\mathbf{0}, \mathbf{I}),
$$

после чего через собственное разложение ковариационной матрицы

$$
\mathbf{C} = \mathbf{B}\,\mathbf{D}^2\,\mathbf{B}^{\mathsf T}
$$

строится вектор

$$
\mathbf{y}_k = \mathbf{B}\mathbf{D}\mathbf{z}_k.
$$

Кандидат в нормализованном пространстве вычисляется как

$$
\mathbf{x}_k = \mathbf{m} + \sigma \mathbf{y}_k.
$$

После этого каждая координата ограничивается диапазоном $[0,1]$:

$$
\mathbf{x}_k \leftarrow \operatorname{clip}(\mathbf{x}_k, 0, 1).
$$

### 5.4. Взвешенная рекомбинация

После вычисления fitness вся популяция сортируется по возрастанию значения целевой функции. Новое среднее определяется взвешенной рекомбинацией лучших $\mu$ особей:

$$
\mathbf{m}^{(t+1)} = \sum_{i=1}^{\mu} w_i \, \mathbf{x}_{i:\lambda},
$$

где $\mathbf{x}_{i:\lambda}$ — $i$-я лучшая особь, а веса $w_i$ — логарифмические и нормализованные:

$$
 w_i \propto \ln(\mu + 0.5) - \ln i,
 \qquad
 \sum_{i=1}^{\mu} w_i = 1.
$$

Эффективное число родителей задаётся как

$$
\mu_{\mathrm{eff}} = \frac{1}{\sum_{i=1}^{\mu} w_i^2}.
$$

### 5.5. Эволюционный путь для шага

Адаптация глобального шага использует эволюционный путь $\mathbf{p}_\sigma$:

$$
\mathbf{p}_\sigma \leftarrow (1-c_\sigma)\mathbf{p}_\sigma
+ \sqrt{c_\sigma(2-c_\sigma)\mu_{\mathrm{eff}}}
\, \mathbf{C}^{-1/2}
\frac{\mathbf{m}^{(t+1)}-\mathbf{m}^{(t)}}{\sigma}.
$$

После этого шаг обновляется по правилу cumulative step-size adaptation:

$$
\sigma \leftarrow \sigma \, \exp\!\left(
\frac{c_\sigma}{d_\sigma}
\left(
\frac{\|\mathbf{p}_\sigma\|}{\chi_n} - 1
\right)
\right),
$$

где $\chi_n$ — ожидаемая длина случайного гауссовского вектора размерности $n$.

### 5.6. Эволюционный путь для ковариации

Путь для ковариационной матрицы имеет вид

$$
\mathbf{p}_c \leftarrow (1-c_c)\mathbf{p}_c
+ h_\sigma \sqrt{c_c(2-c_c)\mu_{\mathrm{eff}}}
\frac{\mathbf{m}^{(t+1)}-\mathbf{m}^{(t)}}{\sigma},
$$

где $h_\sigma \in \{0,1\}$ — индикатор устойчивости длины эволюционного пути.

### 5.7. Обновление ковариационной матрицы

Ковариационная матрица обновляется как сумма rank-one и rank-$\mu$ вкладов:

$$
\mathbf{C} \leftarrow
(1-c_1-c_\mu + \delta_h c_1)\mathbf{C}
+ c_1\mathbf{p}_c\mathbf{p}_c^{\mathsf T}
+ c_\mu \sum_{i=1}^{\mu} w_i \mathbf{y}_{i:\lambda}\mathbf{y}_{i:\lambda}^{\mathsf T},
$$

где

$$
\mathbf{y}_{i:\lambda} = \frac{\mathbf{x}_{i:\lambda} - \mathbf{m}^{(t)}}{\sigma}.
$$

Здесь rank-one update отвечает за накопленное направление успешного поиска, а rank-$\mu$ update — за локальную геометрию лучших решений текущего поколения.

### 5.8. Критерии остановки

Оптимизация завершается при выполнении хотя бы одного из условий:

1. достигнут лимит вычислений fitness;
2. достигнут лимит поколений;
3. достигнуто целевое значение fitness;
4. глобальный шаг стал слишком мал:

$$
\sigma < 10^{-20}.
$$

---

## 6. Связь между параметрами оптимизации и геометрией

После нахождения лучшего решения

$$
\mathbf{a}^* = (a_1^*, \dots, a_n^*),
$$

каждый коэффициент трактуется как смещение соответствующего подвижного узла вдоль его нормали:

$$
\Delta \mathbf{r}_i = a_i^* \hat{\mathbf{n}}_i.
$$

Тем самым итоговая геометрия имеет вид

$$
\mathbf{r}_i^{\,\text{opt}} = \mathbf{r}_i + a_i^* \hat{\mathbf{n}}_i.
$$

Это полностью согласовано с тем же форматом displacement-представления, который используется для smoother’а.

---

## 7. Полный вычислительный pipeline

С практической точки зрения используемый в проекте цикл выглядит так:

1. загрузка исходной сетки;
2. извлечение допустимых границ и нормалей через `bs-shift-boundary`;
3. построение пространства допустимых коэффициентов $[a_i^{\min}, a_i^{\max}]$;
4. запуск CMA-ES в нормализованном пространстве $[0,1]^n$;
5. для каждого кандидата — вызов внешнего `bs-form-opt` и чтение fitness;
6. после завершения — сохранение лучшего вектора коэффициентов;
7. экспорт displacement JSON для визуализации в UI.

Схема процесса:

$$
\text{mesh}
\rightarrow
\texttt{bs-shift-boundary}
\rightarrow
\{\hat{\mathbf{n}}_i, a_i^{\min}, a_i^{\max}\}
\rightarrow
\text{CMA-ES}
\rightarrow
\texttt{bs-form-opt}
\rightarrow
f(\mathbf{x})
\rightarrow
\mathbf{a}^*
\rightarrow
\text{optimized shape / displacement JSON}.
$$

---

## 8. Краткая интерпретация для статьи

Если формулировать максимально компактно, то метод можно описать так.

### 8.1. Сглаживание

Используется constrained Laplacian smoothing, в котором перемещение каждого подвижного узла ограничено направлением его локальной нормали. На каждом шаге вычисляется среднее положение соседних узлов, после чего вектор к этому среднему проектируется на нормаль узла. Для уменьшения усадки поверхности может использоваться второй компенсирующий проход с отрицательным коэффициентом.

### 8.2. Оптимизация формы

Оптимизация формы формулируется как задача минимизации чёрного ящика по коэффициентам нормальных смещений граничных узлов. Допустимые интервалы коэффициентов автоматически извлекаются предварительным расчётом границ смещений. Поиск лучшего вектора коэффициентов ведётся методом CMA-ES в нормализованном пространстве, а значение целевой функции получается из внешнего МКЭ-решателя для каждой сгенерированной геометрии.

---

## 9. Что можно сразу переносить в статью

В статью практически без изменений можно перенести:

- формулы из разделов 2, 3 и 5;
- описание чёрного ящика fitness из раздела 4;
- общий pipeline из раздела 7;
- краткую интерпретацию из раздела 8 как основу для разделов `Methods` или `Algorithm`.

Если потребуется, следующий шаг — развернуть этот текст в полноценный subsection статьи в стиле:

- `Normal-constrained surface smoothing`,
- `Black-box shape optimization`,
- `CMA-ES update equations`,
- `Computational workflow`.
